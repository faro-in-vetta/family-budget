# config.js навмисно НЕ ігнорується.
#
# Щоб сторінка працювала на GitHub Pages, ключ має бути поруч із нею.
# Ключ publishable для цього й призначений: він лише вказує, до якого проєкту
# звертатись, а доступ до даних вирішують політики в базі (supabase/schema.sql),
# перевірені тестом supabase/test-access.sh.
#
# Що сюди класти НЕ МОЖНА ніколи: ключі secret / service_role і пароль бази.
